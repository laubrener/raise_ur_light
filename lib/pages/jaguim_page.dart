import 'package:flutter/material.dart';

class JaguimPage extends StatelessWidget {
  const JaguimPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('JaguimPage'),
      ),
    );
  }
}
